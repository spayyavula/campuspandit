# Local Environment Variables Example
# Copy this file to .env.local and update with your local settings
# .env.local is ignored by git for security

# Supabase Configuration
VITE_SUPABASE_URL="https://your-project-url.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# App Configuration
VITE_APP_TITLE="CampusPandit - Local Development"
VITE_APP_VERSION="1.0.0-local"
VITE_APP_ENVIRONMENT="local"

# API Configuration
VITE_API_BASE_URL="http://localhost:8000/api"
VITE_API_TIMEOUT="5000"

# Development Tools
VITE_ENABLE_DEBUG_MODE="true"
VITE_SHOW_PERFORMANCE_METRICS="true"

# Local Feature Overrides
VITE_ENABLE_GAMING="true"
VITE_ENABLE_OFFLINE_MODE="false"
VITE_ENABLE_PWA_PROMPTS="false"

# Test Data
VITE_USE_MOCK_DATA="true"
VITE_MOCK_USER_ID="test-user-123"

# Payment Gateway Configuration
# IMPORTANT: Secrets are now stored in Supabase Edge Functions for security
# See PAYMENT_EDGE_FUNCTIONS_SETUP.md for deployment instructions
# These client-side variables only control mode and URLs

# PayU Payment Gateway
VITE_PAYU_BASE_URL="https://test.payu.in"  # Use https://secure.payu.in for production
VITE_PAYU_MODE="test"  # test or live

# Razorpay Payment Gateway
VITE_RAZORPAY_MODE="test"  # test or live

# Instamojo Payment Gateway
VITE_INSTAMOJO_BASE_URL="https://test.instamojo.com"  # Use https://www.instamojo.com for production
VITE_INSTAMOJO_MODE="test"  # test or live

# Shopify Payment Gateway
VITE_SHOPIFY_MODE="test"  # test or live

# ⚠️ SECURITY NOTICE ⚠️
# DO NOT add the following to this file (they are server-side only):
# - RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET (set via: supabase secrets set)
# - PAYU_MERCHANT_KEY and PAYU_MERCHANT_SALT (set via: supabase secrets set)
# - INSTAMOJO_API_KEY and INSTAMOJO_AUTH_TOKEN (set via: supabase secrets set)
# - SHOPIFY_STORE_DOMAIN and SHOPIFY_STOREFRONT_ACCESS_TOKEN (set via: supabase secrets set)
# - SHOPIFY_PRODUCT_ID (optional, set via: supabase secrets set)
#
# To configure payment gateway secrets:
# 1. Install Supabase CLI: npm install -g supabase
# 2. Login: supabase login
# 3. Link project: supabase link --project-ref YOUR_PROJECT_REF
# 4. Set secrets: supabase secrets set RAZORPAY_KEY_ID=your_key
# 5. Deploy functions: supabase functions deploy
#
# See PAYMENT_EDGE_FUNCTIONS_SETUP.md for complete instructions
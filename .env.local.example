# Local Environment Variables Example
# Copy this file to .env.local and update with your local settings
# .env.local is ignored by git for security

# Supabase Configuration
VITE_SUPABASE_URL="https://your-project-url.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# App Configuration
VITE_APP_TITLE="CampusPandit - Local Development"
VITE_APP_VERSION="1.0.0-local"
VITE_APP_ENVIRONMENT="local"

# API Configuration
VITE_API_BASE_URL="http://localhost:8000/api"
VITE_API_TIMEOUT="5000"

# Development Tools
VITE_ENABLE_DEBUG_MODE="true"
VITE_SHOW_PERFORMANCE_METRICS="true"

# Local Feature Overrides
VITE_ENABLE_GAMING="true"
VITE_ENABLE_OFFLINE_MODE="false"
VITE_ENABLE_PWA_PROMPTS="false"

# Test Data
VITE_USE_MOCK_DATA="true"
VITE_MOCK_USER_ID="test-user-123"

# Payment Gateway Configuration
# IMPORTANT: Secrets are stored in Supabase Edge Functions for security
# See STRIPE_INTEGRATION_GUIDE.md for complete setup instructions
# See PAYPAL_SETUP_GUIDE.md for PayPal configuration

# Stripe Payment Gateway (Client-side Publishable Key - Safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key_here"  # Use pk_live_xxx for production
VITE_STRIPE_MODE="test"  # test or live

# PayPal Support
# PayPal is integrated through Stripe - no additional configuration needed here!
# To enable PayPal:
# 1. Go to Stripe Dashboard → Settings → Payment methods
# 2. Enable "PayPal" and connect your PayPal Business account
# 3. PayPal will automatically appear as a payment option for customers
# See PAYPAL_SETUP_GUIDE.md for detailed instructions

# ⚠️ SECURITY NOTICE ⚠️
# DO NOT add the following to this file (they are server-side only):
# - STRIPE_SECRET_KEY (set via: supabase secrets set STRIPE_SECRET_KEY=sk_test_xxx)
#
# To configure Stripe secrets:
# 1. Get your API keys from: https://dashboard.stripe.com/test/apikeys
# 2. Install Supabase CLI: npm install -g supabase
# 3. Login: supabase login
# 4. Link project: supabase link --project-ref YOUR_PROJECT_REF
# 5. Set secret key: supabase secrets set STRIPE_SECRET_KEY=sk_test_your_secret_key
# 6. Deploy Edge Functions: supabase functions deploy create-stripe-payment-intent
# 7. Deploy Edge Functions: supabase functions deploy verify-stripe-payment
#
# For production:
# - Use pk_live_xxx for VITE_STRIPE_PUBLISHABLE_KEY
# - Use sk_live_xxx for STRIPE_SECRET_KEY
# - Set VITE_STRIPE_MODE="live"